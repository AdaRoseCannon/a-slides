{
  "name": "aslides",
  "private": true,
  "version": "1.2.4",
  "description": "A-Slides",
  "scripts": {
    "build:js": "webpack -d -p --optimize-dedupe",
    "build:sass": "node-sass ./sass/a-slides.scss -o ./build/",
    "build": "npm run build:js && npm run build:sass"
  },
  "devDependencies": {
    "babel-core": "^6.7.4",
    "babel-loader": "^6.2.4",
    "babel-plugin-transform-runtime": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "hammerjs": "~2.0.4",
    "marked": "~0.3.3",
    "node-sass": "^3.4.2",
    "peerjs": "~0.3.14",
    "sw-toolbox": "^2.0.4",
    "webpack": "^1.12.14"
  },
  "engines": {
    "node": ">=0.8.0"
  }
}
